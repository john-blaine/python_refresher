movies_watched = { 'Forrest Gump', 'Inside Out', 'Coraline' }

user_movie = input('Enter something you want to watch: ')

print(user_movie in movies_watched)

# This also works for checking if a substring is inside a string
